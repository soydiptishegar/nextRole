### Job application, using ReactJS and Golang (comming soon)!!!

**CSS Tools :**
* Tailwind CSS
* postcss, autoprefixer

**Commands :**
`cd job-app` 
`make start-ui #Run the Frontend`

**Step-wise implementation :**
* Setup the project using Vite (frontend toolkit), Tailwind, PostCSS & Autoprefixer.
* Add Basic Components, Site setup using AI
* Load the data from jobs.json
* Add routing
* loading - Created the JobsPreviewSkeleton.jsx loads this before data
* empty states

* Add Job Detail Page
* Job


Next ChatGPT todos

👉 Add Job Detail page (/jobs/:id)
👉 Reuse same data without refetching
👉 Wire “Read more → Job detail”